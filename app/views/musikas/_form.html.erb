<%= form_with(model: musika, local: true) do |form| %>
  <% if musika.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(musika.errors.count, "error") %> prohibited this musika from being saved:</h2>

      <ul>
        <% musika.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_area :title %>
  </div>

  <div class="field">
    <%= form.label :singer %>
    <%= form.text_field :singer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
